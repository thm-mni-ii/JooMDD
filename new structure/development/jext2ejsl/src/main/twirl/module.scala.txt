@(module: de.thm.icampus.mdd.model.extensions.ModuleExtension)
Module @{module.name} {
    @manifest(module.manifest)
    languages {
        @for(lang <- module.languages) {
            @language(lang)
        }
    }
    @if(module.page.nonEmpty) {
        *Page module.page
    }
}