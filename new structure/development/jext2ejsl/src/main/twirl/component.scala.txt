@(component: de.thm.icampus.mdd.model.extensions.ComponentExtension)
Component @{component.name} {
    @manifest(component.manifest)
    languages {
        @for(lang <- component.languages) {
            @language(lang)
        }
    }
    sections {
        Frontend section {
            *Pages @{component.frontend.pages.map(_.name).mkString(", ")}
        }
        Backend section {
            *Pages @{component.backend.pages.map(_.name).mkString(", ")}
        }
    }
}