@(clazz: de.thm.icampus.mdd.model.oo.Clazz)
Class @{clazz.name} {
    methods {
        @for(method <- clazz.methods) {
            Method @{method.name} {
                @if(method.returnType.isDefined) {
                    Returnvalue result:@{method.returnType.get}
                }
                @if(method.params.nonEmpty) {
                    methodparameters {
                        @for(param <- method.params) {
                            MethodParameter @{param.name}:@{param.dataType}
                        }
                    }
                }
            }
        }
    }
}