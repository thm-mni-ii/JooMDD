@(p: de.thm.icampus.mdd.model.extensions.JParam)
Parameter @{p.name} {
    type = "@{p.htmltype}"
    @if(p.default.isDefined) {
    defaultvalue = "@{p.default.get}"
    }
    label = "@{p.label}"
    @if(p.size.isDefined) {
    size = @{p.size.get}
    }
    description = "@{p.description}"
}