@(folder: de.thm.icampus.mdd.model.oo.Folder)
Package @{folder.name} {
    @if(folder.structs.nonEmpty) {
        classes {
            @for(struct <- folder.structs) {
                @struct match {
                    case c: de.thm.icampus.mdd.model.oo.Clazz => {
                        @clazz(c)
                    }
                    case i: de.thm.icampus.mdd.model.oo.Interface => {}
                }
            }
        }
    }
    @if(folder.folders.nonEmpty) {
        packages {
            @for(subfolder <- folder.folders) {
                @txt.folder(subfolder)
            }
        }
    }
}